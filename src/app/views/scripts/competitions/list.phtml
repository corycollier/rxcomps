<?php
/**
 * list view script
 *
 * This view script is the list page for all competitions
 *
 * @category    RxCompetition
 * @package     App
 * @subpackage  View_Scripts
 * @copyright   Copyright (c) 2012 RxCompetition.com, Inc (http://www.RxCompetition.com)
 * @license     All Rights Reserved
 * @version     Release: 1.0.0
 * @since       File available since release 1.0.0
 */
?>

<?php
$eventId = $this->request()->getParam('event_id');

// echo the appropriate link to create a new competition
echo $this->model(new App_Model_Competition)->create($this->user, 'Add Competition', array(
    'event_id'  => $eventId,
));

// iterate through the competitions, rendering information
foreach ($this->items as $competition) :

    echo $this->competition($competition)->item($this->user, array(
        'event_id'  => $eventId,
    ));

endforeach;

// output any pagination control necessary
echo $this->paginationControl($this->paginator, 'Sliding', 'partials/pagination.phtml');

?>
