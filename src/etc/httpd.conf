
<VirtualHost *:80>
    ServerName dev.rxcompetition.com

    DirectoryIndex index.php

    DocumentRoot /Users/corycollier/Sites/rxcompetition.com/src/pub

    <IfModule mod_php5.c>
        SetEnv APPLICATION_ENV "development"
    </IfModule>

    <Directory />
        AllowOverride All
        Allow From All
        Order allow,deny
    </Directory>

    <IfModule mod_rewrite.c>
        RewriteEngine On
    </IfModule>

</VirtualHost>
